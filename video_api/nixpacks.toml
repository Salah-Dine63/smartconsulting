[phases.setup]
nixPkgs = ["python311", "ffmpeg", "chromium"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"
